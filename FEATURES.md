# Smart Trip 功能规格说明书 (Full Feature Specification)

本文档详细列出了 Smart Trip 智能旅行规划应用中已实现的所有核心功能与交互细节。应用旨在通过极致的 UI 设计与智能化的 API 集成，为用户提供沉浸式的旅行规划体验。

---

## 1. 基础架构与安全性 (Core & Security)
*   **ES Module 模块化架构**：全站代码采用 ES6 模块拆分，逻辑与渲染分离，确保高性能加载与易维护性。
*   **全局错误监控**：搭载全站级 Error Boundary，当发生 JavaScript 运行时错误时，会自动弹出美观的错误诊断界面，并提供“一键修复/重置”选项。
*   **响应式登录系统**：
    *   极简的 Mock 登录流程，支持回车键快捷登录。
    *   自动识别并保存用户信息，桥接看板与行程详情视图。

## 2. 智能仪表盘中心 (Dashboard Hub)
*   **多维度筛选过滤 (Filtering)**：
    *   **全部行程**：一键查看库中所有规划。
    *   **进行中 (Ongoing)**：基于当前服务器时间自动识别或手动指定的即时行程。
    *   **计划中 (Planned)**：管理未来未开始的冒险。
    *   **已完成 (Completed)**：回顾珍贵的过往回忆。
*   **视图切换矩阵 (View Toggles)**：
    *   **网格视图 (Grid)**：针对精美配图进行沉浸式展示。
    *   **列表视图 (List)**：数据高密度排列，补全了与网格视图一致的“更多操作”菜单（编辑、分享、删除）。
*   **实时财务汇总**：看板行程卡片直观显示该行程所有站点的累计费用（Total Cost），金额实时更新。
*   **图片缓存清理**：内置管理员工具，可一键扫描服务器并清理已失效或孤立的本地缓存图片。

## 3. 沉浸式行程详情区 (Itinerary Engine)
*   **侧边栏灵动交互**：
    *   **全功能折叠**：支持侧边栏一键收缩，折叠后显示简洁的天数标签（D1, D2）。
    *   **鼠标跟随底光 (Glow Tracking)**：折叠状态下，鼠标靠近按钮时，按钮周围泛起柔和的蓝色光晕，光晕强度随距离动态衰减。选中的天数按钮始终保持常驻底光。
    *   **独立光影层渲染 (Separate Glow Layer)**：光影效果通过独立的底层 DOM 容器渲染（z-index: 0），位于所有按钮（z-index: 2）的下方，确保光影永远不会遮挡任何按钮。
    *   **高性能渲染引擎**：采用 `requestAnimationFrame` 节流、DOM 引用缓存、位置缓存等优化策略，确保 60fps 丝滑跟随。
    *   **快速锚点跳转**：点击侧边栏天数，页面平滑滚动至对应日程，侧边栏状态同步高显。
*   **垂直虚线时间轴**：像素级对齐的视觉导流设计，将每日行程串联成连贯的行为流。
*   **多维天数管理**：
    *   **自定义天数主题色**：每一天都支持独立配色。
    *   **天数副标题编辑**：支持实时编辑日程标题。
*   **智能交通计算引擎 (Transit Engine) & 汇总**：
    *   **高精度汇总引擎**：利用正则表达式解析历史路程字符串，结合实时计算的数值，准确汇总每日总时长与总里程，数据随语言与单位设置动态切换。
    *   **Labeled Switch 深度定制**：在汇总数据后方集成宽轨 Switch 切换开关，将“显示回程 (Return Route)”文字标签直接整合进开关轨道内部，滑块移动时文字自动偏移，交互更符合现代前卫审美。
    *   **自动分段路程**：卡片之间自动触发路程与时长计算，支持 🚗 (驾车) 与 🚶 (步行) 切换。
*   **统一住宿可视化系统 (Unified Hotel Stay Visualization)**：
    *   **全局协同高亮 (Synchronized Highlights)**：鼠标悬停在酒店卡片、边框、插座或任意一段住宿连线上，整段行程（跨越全天）的相关元素会同步进入“点亮”状态（亮橙色 + 柔和光晕）。
    *   **无缝贯穿订阅线 (Continuous Stay Line)**：垂直订阅线不再被天数标题中断。通过超长垂直延伸技术（10rem Overreach），线条在视觉上完美穿透天数标题、日期工具栏与留白区域，形成逻辑统一的长连线。
    *   **状态语义化识别**：酒店卡片顶部标签采用独立配色方案——“入住 (Check-in)”为暖黄色，“退房 (Check-out)”为警示红，与住宿线的主体色（暗灰橙）形成对比，增强导视性。
    *   **自动补全逻辑**：即便在没有任何行程的“空白日期”，只要该日期处于住宿期内，背景中依然会自动绘制贯穿的订阅线，保持视觉流的完整性。
*   **动态快捷插入**：在任意两个地点之间，悬停即可触发“+”按钮，实现原地精准插入。


## 4. Google Maps 深度集成 (Map Intelligence)
*   **主题化路线渲染**：仅绘制当前展开天的路网，颜色同步自定义主题色。
*   **虚拟航段补全 (Route Injection)**：地图自动绘制从酒店到首个地点的连线。
*   **按需回程显示**：通过日程单底部的 Switch 开关，一键控制地图回程连线的显示/隐藏。
*   **全系统国际化 (Universal i18n)**：全站 UI 标签、工具提示、分类名称及系统自动生成的备注已全面国际化。支持一键切换中/英文，且所有单位（KM/MI, C/F）同步转换。
*   **全区域点击响应**：定制地图标记支持全区域点击（包括中心的数字与球体部分），修复了交互死角。
*   **旧数据自动降级检索 (Search Fallback)**：针对没有预存 `placeId` 的历史行程站点，点击地标时会自动通过名称与经纬度触发智能检索，确保所有历史数据均能正常弹出详情面板。
*   **POI 实时预览悬停窗 (Sophisticated Hover Info)**：
    *   **极简横向双栏排版**：左侧展示地点核心信息（店名、分类、评分、地址、电话），右侧为全高度覆盖的实拍缩略图。
    *   **智能排版保护**：店名过长时自动缩放字体大小，确保类型与评分标签永远处于单行水平对齐，不发生换行挤占。
    *   **极致暗色同步**：采用 `#0d111b` 深蓝黑背景与 `invert(100%)` 滤镜对消技术，实现悬停窗与主详情面板颜色的像素级视觉一致。
    *   **酒店/住宿专属定制**：自动识别并关联行程中的住宿期限，展示入住/退房具体日期时间、停留晚数及总预估费用。
    *   **高对比度白昼模式**：针对白天模式优化了时间与价格标签的配色（深紫/深绿），确保在纯白背景下的绝佳读认性。
    *   **全向溢出保护**：内置智能位移算法，根据地标在屏幕中的象限位置自动切换弹出方向（上弹/下弹/位移），确保信息永不超出视口。
*   **智能换图子系统**：
    *   集成 **Google Places Photo API**。
    *   点击卡片图片即可在海量实拍图中搜索并预览。
    *   支持单选高亮确认与双击快速保存。
*   **自定义快捷搜索控件**：地图顶部集成自定义搜索栏，支持按“餐厅”、“酒店”、“景点”等分类进行一键全图打点搜索。
*   **日夜模式切换**：支持 ☀️ 浅色 / 🌙 极客深色两种地图皮肤实时切换。
*   **状态指示器**：左下角指示器实时反馈地图 API 加载、标记点同步及数据包状态。

## 5. 极致 UI/UX 体验 (Advanced UX Design)
*   **毛玻璃美学 (Glassmorphism)**：全站 UI 深度采用背景模糊与半透明边框设计，营造高级感。
*   **智能拖拽排序 (Drag & Drop)**：
    *   行程卡片支持任意拖拽重排顺序。
    *   **边缘自动滚动 (Auto-scroll)**：长页面拖拽时屏幕会自动滚动，无需松手。
    *   **局部动效更新**：排序仅刷新受影响的 DOM 节点，避免页面闪烁。
*   **自适应输入系统**：所有文本域（笔记、单据、备注）均支持随着内容增多自动撑开高度。
*   **费用与时间智能选择**：
    *   **消费追踪**：支持快速设置消费金额，输入框点击自动全选，并提供全品类图标选择（餐饮、购物、交通等）。
    *   **时间选择器**：集成高度定制的 Flatpickr 日历与时间选择插件。
*   **零闪烁局部渲染 (Partial Update)**：
    *   在切换仪表盘标签或视图模式时，仅对行程内容区进行 DOM 补丁级更新，页面核心元素保持静止，操作无任何延迟感。
*   **位置记忆系统**：在保存、换图、重排等所有关键点击操作后，页面完美保持在当前的滚动位置，确保操作连续性。
*   **轻量级弹窗体系**：摒弃系统级弹窗，所有 Modal 均采用非模态/侧滑设计，不中断操作心流。

## 6. 后端服务与持久化 (Backend & API)
*   **JSON 数据库引擎**：底层使用 `db.json` 作为存储介质，天然适配 Git 版本控制，支持多端数据同步。
*   **图片本地化镜像服务器**：
    *   `POST /api/upload-image`：将远程 Google 图片拉取并转存至本地。
    *   `POST /api/upload-local`：完整支持从本地电脑直传 PNG/JPG/WebP 图片。
    *   **自适应封面算法**：服务端配合前端 `object-fit: contain` 与高斯模糊背景技术，解决不同比例图片导致的拉伸或剪裁问题。
*   **自动同步机制**：任何 UI 上的关键修改（增删改查）均会毫秒级同步至后端文件系统，防止数据丢失。

---
*Generated by Antigravity AI Assistant | 2026-03-11*
