# Smart Trip 功能规格说明书 (Full Feature Specification)

本文档详细列出了 Smart Trip 智能旅行规划应用中已实现的所有核心功能与交互细节。
应用旨在通过极致的 UI 设计与智能化的 API 集成，为用户提供沉浸式的旅行规划体验。

---

## 1. 基础架构与安全性 (Core & Security)

- **ES Module 模块化架构**：全站代码采用 ES6 模块拆分，逻辑与渲染分离，确保高性能加载与易维护性。
- **全局错误监控**：搭载全站级 Error Boundary，当发生 JavaScript 运行时错误时，会自动弹出美观的错误诊断界面，并提供"一键修复/重置"选项。
- **Supabase 云端认证**：基于 Supabase Auth 的用户注册/登录系统，持久化 Session，页面刷新后自动恢复登录状态。

---

## 2. 全自动国际化与单位定制 (i18n & Settings Persistence)

- **多语言引擎**：全站基于 `en.json` / `zh.json` 实现中英双语支持，一键无感切换。
- **设置自动持久化**：
  - 用户设置（语言、货币、温度、距离单位）保存至 Supabase `user_settings` 表。
  - 登录后自动读取并应用用户设置，**渲染前先加载语言包**，无闪烁切换。
  - 语言偏好同步写入 `localStorage`，下次冷启动即时生效，无需等待 Supabase 响应。
- **全深度去硬编码**：全站 UI 标签、工具提示、分类名称、系统备注均已 100% 国际化，无残留中文硬编码。
- **单位与货币系统**：支持用户自定义 公里/英里 (KM/MI)、摄氏度/华氏度 (C/F) 以及多种货币单位。

---

## 3. 智能仪表盘中心 (Dashboard Hub)

- **多维度筛选过滤**：支持按"全部"、"进行中"、"计划中"、"已完成"状态实时筛选行程。
- **视图切换矩阵**：网格视图 / 列表视图一键切换，列表模式包含完整的编辑/分享/删除菜单。
- **实时财务汇总**：看板卡片直观显示所有站点的累计费用，金额实时更新。
- **图片缓存清理**：内置管理工具，可一键扫描并清理已失效或孤立的 Supabase Storage 图片。

---

## 4. 行程 CRUD 与数据持久化 (Trip Management)

- **新建行程默认值智能化**：
  - 入住日期默认为**当天**，退房日期默认为**5天后**，自动生成对应天数的日程结构。
  - 封面图片默认展示"旅行 / Trip"关键词的图片（中文用旅行，英文用 Trip），不再显示默认 Unsplash 图。
- **行程删除（含图片清理）**：
  - 删除行程时同步从 Supabase `trips` 表删除数据行。
  - 自动收集行程下所有 Supabase Storage 图片（封面 + 各停靠点），批量清理以避免孤立文件占用空间。
  - 外部图片链接（Unsplash / loremflickr 等）不受影响。
- **地址卡删除**：采用"懒删除"策略，图片通过"清理图片缓存"按钮统一清理，避免误删无法恢复。
- **行程元数据编辑**：支持标题、封面图、状态、起止日期的全面编辑，修改日期可自动延伸/缩短天数。

---

## 5. 沉浸式行程详情区 (Itinerary Engine)

- **侧边栏灵动交互**：折叠/展开、鼠标跟随底光、快速锚点跳转、点击天数自动展开/折叠。
- **垂直虚线时间轴**：连续时间轴串联每日行程，住宿期内自动绘制贯穿订阅线。
- **多维天数管理**：自定义天数主题色、副标题编辑、清空当天、添加日期。
- **零闪烁局部渲染**：添加/删除项目、保存住宿信息、增删天数时仅刷新受影响的 DOM 节点，不再刷新整个页面。
- **跨天智能拖拽排序 (V3)**：
  - 基于 Pointer Events API 的全新拖拽引擎，支持跨天拖拽排序。
  - 所有天的卡片视为单一扁平列表，30% 重叠阈值触发 iOS 风格 CSS Transform 位移动画。
  - 5px 拖拽阈值延迟激活，保留子元素（时间芯片、消费、照片）的正常点击事件。
  - 空天幽灵占位符支持拖入无行程的天。

---

## 6. 智能交通计算引擎 (Transit Engine)

- **实时路程分析**：自动计算地点间的驾车/步行路程与时长，基于 Google Routes v2 REST API。
- **Duration 格式修复**：正确解析 Routes v2 API 返回的 `"1234s"` 字符串格式（去除 `s` 后缀后解析），彻底修复"永远在计算中"的显示 Bug。
- **坐标验证**：计算前校验坐标有效性，无效坐标优雅降级并显示"暂无数据"而非永久 Loading。
- **交通模式切换**：支持 🚗 驾车 / 🚶 步行 切换，Labeled Switch 控制每日回程显示。
- **酒店虚拟路段**：自动补全从酒店到首个地点 / 最后地点到酒店的交通提示。

---

## 7. 统一住宿可视化系统 (Hotel Stay Visualization)

- **全局协同高亮**：悬停缩略图或相关卡片时，整段住宿逻辑同步激活（亮橙色高亮）。
- **清爽视觉风格**：移除了多余的棕褐色垂直订阅连线，消除了地址卡左侧的蓝色/棕色竖条视觉干扰，界面更加现代简洁。
- **状态语义化识别**：入住为暖黄色，退房为警示红，增强导视性。

---

## 8. Google Maps 深度集成 (Map Intelligence)

- **主题化路线渲染**：仅绘制当前展开天的路网，颜色同步自定义主题色；重排站点顺序或更换天数颜色后地图即时响应。
- **全区域点击响应**：地图标记支持全区域点击（包括数字与球体部分），无交互死角。
- **搜索面板**：地图顶部集成自定义分类搜索（餐厅、景点、酒店等），支持键盘上下键导航推荐列表，支持"添加到行程"。
- **日夜模式切换**：☀️/🌙 两种地图皮肤实时切换。
- **POI 悬停信息窗**：双栏排版（信息 + 实拍图），酒店卡片额外展示入退住日期与费用。
- **街景图自动填充**：地点卡片与信息面板无 Google Photos 时，自动异步获取街景截图作为封面，体验与 Google Maps 一致。

---

## 9. 费用追踪系统 (Expense Tracking)

- **多分类图标选择**：支持 12 类费用分类（航班、住宿、餐饮、购物、交通、活动、汽油、杂货等），含图标视觉选择器。
- **分类显示完全本地化**：选择分类后，展示名称根据当前语言自动翻译（不再显示中文原始 key）。
- **付款人 / 分摊**：支持查看付款人信息与分摊设置。
- **日期自动解析**：智能解析中英文日期格式（"Mar 11, 2026" / "3月11日"），正确显示 MM/DD。

---

## 10. 极致 UI/UX 体验 (Advanced UX Design)

- **毛玻璃美学 (Glassmorphism)**：全站深度采用背景模糊与半透明边框。
- **自适应输入系统**：所有文本域随内容自动撑高。
- **地图渲染零闪烁**：引入 `requestAnimationFrame` 和状态差分机制，彻底解决添加/删除地址时的地图跳动与刷新闪烁问题。
- **零闪烁局部渲染**：切换标签/视图或增删行程项时，仅对页面受影响区域进行外科手术式 DOM 更新。
- **位置记忆系统**：保存/换图/重排等操作后，页面保持当前滚动位置。
- **智能预约时间选择器 (Smart Time Wheel)**：
  - **物理惯性滚动**：基于 CSS Scroll Snap 的高性能时间滚轮，支持随手速变化的物理减速与精准吸附效果。
  - **AM/PM 时段显示**：12 小时制 + AM/PM 标签，30 分钟间隔（48 个时间槽）。
  - **O(1) 高性能滚动**: `scrollTop` 数学计算直接定位中心项，`requestAnimationFrame` 节流 + 60ms 防抖状态更新，GPU `transform: scale()` 动画。
  - **休息日智能预警**：自动比对预约日期与 Google Business Hours。若撞上地点的休息日/关门日，日期标签与营业详情将同步呈现**动态呼吸红色**预警。
  - **营业时间对照面板**：左侧展示一周营业时间，关门日标红，当天加粗高亮。
  - **日期 + 星期显示**：右侧橘黄色日期徽章下方标注对应星期，关门日星期文字变红。
  - **专业宽屏并排布局**：680px 弹窗排版，左侧参考营业时间，右侧进行时间拨动，互不干扰，同步对照。
- **地址卡关门预警**：
  - 当地址卡所在星期匹配 Google Opening Hours 的关门日时，卡片边框变红 + 顶部显示脉冲呼吸动画 "今天休息!" 警告横幅。
  - 支持中英双语 (`closed_today`)。


---

## 11. 云端后端服务 (Supabase Backend)

- **认证**：Email/Password 注册登录，Session 自动续期。
- **数据存储**：`trips` 表（以 `user_id` 隔离数据）+ `user_settings` 表（个人偏好设置）。
- **文件存储**：`trip-media` Supabase Storage Bucket 存储封面图与站点图片。
- **数据安全**：所有查询携带 `user_id` 过滤，防止越权访问。

---

## 12. 多端自适应响应式布局 (Multi-terminal Adaptive Layout)

- **物理隔离的模块化样式**：采用独立的 `desktop.css` / `tablet.css` / `mobile.css` 模块，通过 HTML 原生 `media` 属性实现按需加载，互不干扰。
- **移动端照片切换器**：地址卡实写图浏览按钮下移并加大，单手操作更便捷，适配大屏手机人体工学。
- **高度解耦的排版逻辑**：各终端排版高度独立，修改桌面端复杂的 Layout 设置不会产生任何移动端回归风险。
- **动态侧栏导航**：侧边栏在移动端自动演化为顶部水平滚动导航，在桌面端支持宽窄状态的手动与自动适配。

---
*Generated by Antigravity AI Assistant | Last updated: 2026-03-17*
