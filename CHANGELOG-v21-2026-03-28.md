# Changelog v21 — 2026-03-28

## Bug Fixes & UI Polish

### 拖拽优化（`useTimelineDrag.js` / `itinerary.css`）
- **修复**：拖拽行程卡片时，仅对 `.stop-card-container` 施加 `translateY` 动画，避免 TransitInfo / 添加按钮跟随卡片一起移动
- **修复**：拖拽结束后正确清理 `.stop-card-container` 的 `transform`、`boxShadow`、`transition` 残留样式
- **新增 CSS**：`.timeline-item-wrapper.dragging .transit-info-row { visibility: hidden }` — 拖拽中的卡片隐藏其下方的交通行，保持布局占位，防止视觉跳动

### TransitInfo 切换模式闪烁修复（`TransitInfo.jsx`）
- **问题**：切换交通模式（驾车 / 步行 / 公共交通）后，新路线数据到达前短暂显示"无路线"
- **修复**：新增 `calculating` 状态 — 模式切换瞬间进入 calculating，显示 `···` 占位符；新路线数据匹配后自动退出；超时 6 秒强制结束，防止永久卡住
- **修复**：统一 SDK 返回值（`DRIVING` / `WALKING` / `TRANSIT`）与 UI 短名（`DRIVE` / `WALK` / `TRANSIT`）的映射，解决 `noRouteForMode` 误判

### 行程卡片排序逻辑（`useTripEditor.js`）
- **修复**：`addStopFromPlace` 添加地点后始终执行 `sortStopsByTime`，不再仅限"追加到末尾"场景，确保带时间的地点始终按时间顺序插入

### 工具栏清理（`DaySection.jsx` / `ItineraryView.jsx`）
- **移除**：每日行程工具栏中的"🪄 Auto Fill"、"📍 Optimize Route (PRO)"、"🕐 Sort by Time" 三个未完成功能按钮
- **简化**：酒店线（HotelLine）不再嵌套在工具栏 wrapper 内，改为独立 `height: 0` 定位锚点，布局更稳定
- **清理**：`onSortByTime` prop 从 `DaySection` 和 `ItineraryView` 中移除；`sortDayByTime` 不再从 `useTripEditor` 解构
