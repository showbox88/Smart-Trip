# 移动端交互与排版深度优化 — 2026-03-20 (Session 3)

> **scope**: 移动端拖拽防误触逻辑重构、响应式排版空间回收、双设备流体验优化
> **files changed**: `DaySection.jsx`, `StopCard.jsx`, `NoteCard.jsx`, `ListCard.jsx`, `TransitInfo.jsx`, `useTimelineDrag.js`, `components.css`, `mobile.css`, `index.css`

---

## 1. 移动端拖拽机制终极重构 (Mobile Drag UX)

由于 Web 引擎（iOS Safari / Android Chrome）原生触摸滚动判断不可中途干预的底层限制，我们在移动端彻底重构了拖拽体系，以保障“丝滑全屏滚动”和“即时无缝拖拽”兼得：

- **引入双侧专属拖动把手 (Ambidextrous Drag Handles)**：在所有的行程卡片（景点/酒店、小记、代办清单）左右两侧新增精美的 `⋮⋮` 拖拽把手图标。
- **免等待零延迟**：取消了手机端体验不佳的“长按 2 秒等待”，捏住把手瞬间即可拖动排序。
- **全屏原生滑动恢复**：全面解除了原先加在卡片主体上的 `touch-action: none` 限制。手机用户将手指放在卡片把手之外的任何区域，都会触发完美的原生页面滑动（滚动），从根本上消灭了拖拽导致页面锁死的卡顿感。

---

## 2. 桌面与平板无感兼容 (Desktop/Tablet Graceful Fallback)

为了不妥协宽屏设备原本的高效操作传统，加入了设备分流判定：
- **屏幕断点判定 (`>= 768px`)**：当用户在电脑或平板（宽度大于等于 768px）使用时，自动隐藏卡片边缘的 6 个点拖拽把手。
- **限制解除**：在宽屏模式下自动跨过移动端的防误触检查，恢复“点击卡片任意空白区域即可起手拖拽”的原版全局交互逻辑。

---

## 3. 手机屏幕黄金展示空间极榨 (Mobile Layout Compression)

考虑到移动端本就极其狭窄的横向阅读宽度，我们将原本被左面长长的时间轴线和点“吃掉”的无用边距挤压至极限：

- **响应式排版弹性化**：将排版彻底从组件内联样式中抽取出来，提取为全局响应式 CSS Variables（如 `--timeline-line-x`, `--card-margin-l`）。
- **左撤平移回收空间**：
  - 时间轴线在手机上向左紧贴边缘靠拢约 12px。
  - 卡片的整体缩进（`Margin-Left`）大幅削减约 16px。
  - 交通组件的 `Padding` 及内部细节统一压缩优化。
  - **总计节省**：为手机端卡片主体硬生生扩宽了近 **30 个像素**（极其可观的展示区域提升），使得地名与笔记能获得更加透气、舒适的展示位而不再早早换行。

---

## 4. 地图详情页（About）与地点卡片手机端精细化排版
针对手机等小屏设备进行了深度的视觉重构与体验升级：
- **About 面板信息重构**：将以前的横向图文排版彻底改为竖向流线型（照片优先置顶），字体全部统一针对手机端做了按比例缩放（`1.35rem -> 1.15rem`），让信息密度更合理舒适。
- **全屏手势相册**：在点击缩略图放大的 lightbox 下，引入了移动端原生的 **左右手势滑动（Swipe）翻页**（利用 `touchstart`/`touchend` 监听与防误触拦截），不再仅依赖左右小箭头。
- **Address Card 宽度克制**：压缩了原本卡片里店名的字号至 `1.05rem`，并缩小了地址电话信息的留白，使得整体卡片在手机短横屏上也能展示全称，缩略图统一成 `80x55px` 的干练大小，让卡片列表整体看起来更加齐整专业。

---

## 5. 移动端高精度定位追踪深度修复 (GPS Geolocation Fix)
由于手机系统中严苛的隐私限制和部分移动网络在默认情况下的迟钝超时表现，重新校准了定位获取的 API 调用参数与回退流程：
- **HTTPS/局域网连接拦截**：增加了明确的无 HTTPS 连结预警拦截，解释“定位在非安全下无效”的原因，避免盲目点击。
- **硬件高精度全量开启**：抛弃了原来低效依赖 WiFi 坐标返回的 `enableHighAccuracy: false`（它在大部分自带浏览器上常常永久超时），将请求超时时间卡在 8000ms 且启用真正的硬件定位。
- **精简交互冲突**：修复了自制对话框“允许”点击后产生的时间差（Temporal Dead Zone）导致的安全授权调用失败问题。

---
*Generated 2026-03-20 by AntiGravity*
