# App Internal Optimization And Modularization - 2026-03-23

> Scope: internal refactor and modularization for Trip / Dashboard / Map related modules without changing product features, page layout, or visual effects.
> Focus: `useTripEditor`, `ItineraryView`, `DashboardPage`, `StayInfoModal`, `MapPanel`, `MapSearchBox`, `MapInfoPanel`

---

## 1. Trip Editor Core Logic

- Refactored `src/hooks/useTripEditor.js`
- Extracted shared trip/day/stop helpers for clone, lookup, insert, sort, and stay-chain handling
- Unified local state update and debounced cloud save flow

New file:
- `src/utils/tripEditorHelpers.js`

## 2. Itinerary View State Split

- Moved local UI state out of `src/components/itinerary/ItineraryView.jsx`
- Centralized modal state, collapsed day state, mobile plan/map mode, pending insertion state, and manual scroll lock handling

New file:
- `src/hooks/useItineraryUIState.js`

## 3. Dashboard Trip Factory

- Extracted default trip creation logic from `src/pages/DashboardPage.jsx`
- Centralized default title, status, colors, dates, and starter content generation

New file:
- `src/utils/tripFactory.js`

## 4. Stay Info Modal Form State

- Split stay form initialization and local state from `src/components/modals/StayInfoModal.jsx`
- Preserved check-in/check-out backfill and save behavior

New file:
- `src/hooks/useStayInfoForm.js`

## 5. Shared Stay Helpers

- Added shared stay aggregation helpers for trip-level and day-level hotel context
- Reused the same stay-derived logic in `DaySection`, `MapPanel`, `formatters`, and stay form code

New file:
- `src/utils/stayHelpers.js`

## 6. Map Panel And Search Stability

- Updated `src/components/itinerary/MapPanel.jsx` to reuse shared stay helpers
- Fixed hotel route logic after stay structure cleanup
- Replaced deprecated `google.maps.places.SearchBox` in `src/components/itinerary/MapSearchBox.jsx` with the newer Places autocomplete flow
- Reduced risk around legacy SearchBox warnings and mount timing issues

## 7. Map Info Panel And Photo Handling

- Split place detail loading into a dedicated hook
- Split lightbox state, keyboard navigation, swipe handling, and image preloading into a dedicated hook
- Unified thumbnail, Photos tab, and lightbox rendering around final photo URL lists
- Restored Street View fallback images for places with no official photos

New files:
- `src/hooks/useMapPlaceDetails.js`
- `src/hooks/useLightboxGallery.js`

## 8. Validation Status

Verified:
- Multiple targeted `eslint` checks passed for touched files
- Manual testing passed for Dashboard, Trip editing, stay modal flows, map search, map info panel, and Street View fallback

Continue observing:
- Long-session map search behavior
- Places with sparse metadata such as missing rating, missing phone, missing photos, or missing opening hours
- Repeated mobile interactions across map and lightbox flows
