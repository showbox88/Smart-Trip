# StayInfoModal 交互重设计 — 2026-03-23

> **scope**: `StayInfoModal` 入住信息弹窗 UI 与交互全面重构
> **files changed**: `src/components/modals/StayInfoModal.jsx`

---

## 1. 滚轮时间选择器 (Scroll-Wheel TimePicker)

**变更**：时间输入由文本框改为自定义 `TimePicker` 组件。鼠标悬停在小时或分钟区域时，滚动鼠标滚轮即可调节时间（小时 ±1，分钟 ±5）。悬停区域为整块 flex 单元，面积大，操作精准。

- 小时/分钟各占 `flex: 1`，高度撑满父容器（48px）
- 悬停时高亮背景 `rgba(255,255,255,0.12)`，光标变 `ns-resize`，有明确的可交互提示

---

## 2. AM/PM 点击切换 (PeriodToggle)

**变更**：AM/PM 选择由 `<select>` 下拉改为点击切换按钮，每次点击在 AM/PM 之间切换，hover 有高亮反馈。

---

## 3. Fieldset Legend 边框标题 (Fieldset Legend Layout)

**变更**：Check-in / Check-out 两组控件分别用原生 `<fieldset>` + `<legend>` 包裹，`CHECK-IN` / `CHECK-OUT` 标题自然嵌入在边框线上（浏览器原生 legend 效果），上下内边距对称。边框为浅蓝色调 `rgba(100,160,255,0.2)`，背景 `rgba(100,160,255,0.06)`。

---

## 4. 日期选择框与时间框样式统一

- `<select>` 固定高度 48px，与时间框等高
- 字体大小 `0.95rem`、字重 `600`、`fontFamily: inherit`，与 TimePicker 完全一致

---

## 5. 代码质量优化

- `pad`、`SELECT_STYLE`、`FIELDSET_STYLE`、`LEGEND_STYLE` 提升为模块级常量，避免每次渲染重新分配
- 初始化逻辑抽取为 `initStayState(trip, stopId)` 函数，通过 `useState` lazy initializer 仅执行一次，消除原先的重复 `flatMap` + `find` 调用
- 6 个独立 `useState` 合并为单一 `useState` 对象 + setter 包装，初始化只调用一次 `initStayState`
- 删除未使用的 `dayId` prop、`labelStyle`、`inputStyle`、`sectionStyle`
