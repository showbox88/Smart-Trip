# 地址卡排版优化与对齐改进 — 2026-03-25

> **scope**: 地址卡排版优化、图片尺寸及对齐调整、功能修复
> **files changed**: `StopCard.jsx`, `itinerary.css`

---

## 1. 地址卡排版优化 (Stop Card Layout)

### 精确对齐逻辑
- **顶部对齐**: 重构了 `StopCard` 的 JSX 结构，使图片顶部与地点名称（Title Row）严格对齐。
- **底部对齐**: 通过 `align-self: stretch` 确保图片底部与最下方的“导航”按钮水平齐平。
- **层级调整**: 将酒店标签（Hotel badge）和营业状态提醒（Closed warning）移至名称上方，确保图片对齐起点的视觉一致性。

### 图片尺寸调优
- **网页端增强**: 图片宽度从 `100px` 显著增加到 `220px`。
- **一致性保护**: 添加了 `max-height: 180px` 限制，防止因内容过长导致图片过度纵向拉伸，兼顾美观与对齐。

---

## 2. 功能修复与细节改进 (Fixes & UX)

### 换图功能修复
- **功能找回**: 修复了在布局重构过程中意外丢失的“点击图片更换/上传”功能菜单。
- **Portal 恢复**: 重新挂载了 `PhotoPickerDropdown` 门户，确保点击图片能正常弹出选择器。

### 冗余名称清理
- **智能去重**: 自动检查 `stop.note` 与 `stop.location`；若内容完全一致，则隐藏备注，避免“双重标题”感。

### 酒店按钮重排
- **布局分区**: 将酒店卡片的“入住信息”按钮提升至独立行，位于操作底栏上方，优化了信息密度。

---

## 模块文件清单

| 模块 | 文件 |
|------|------|
| **行程组件** | `src/components/itinerary/StopCard.jsx` |
| **行程样式** | `src/styles/itinerary.css` |
