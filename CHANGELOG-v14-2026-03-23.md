# Archive Sync & Album UX 改进 — 2026-03-23

> **scope**: Trip Archive 同步逻辑修复、相册视图重构、右键菜单滚动/选中修复
> **files changed**: `useArchiveSync.js`, `TripAlbumGrid.jsx`, `App.jsx`(archive), `ContextMenu.jsx`, `useContextMenu.js`

---

## 1. 同步逻辑修复 (Sync Logic)

### Trip 匹配改为 ID-only
- 之前按 `title` fallback 匹配，导致同名 trip（如多个 "My New Trip"）全部合并到一个 album
- 现在只用 `trip_id === stTrip.id` 精确匹配，每个 Smart Trip 独立创建 album

### 深拷贝 & 磁盘读取
- `syncToArchive` 改为从磁盘重新读取 `trip_database.json`，避免使用 React state 中的过期数据
- 数组深拷贝（`[...arr]`）替代浅拷贝（`{ ...obj }`），防止 `push()` 直接修改 React state

### Event 匹配优化
- 优先用 `stop.id === event_id` 匹配，fallback 到 `title + trip_id`
- 更新已有 event 的 city/date/lat/lng 等核心字段

---

## 2. 相册视图重构 (TripAlbumGrid)

### 仅显示已同步的 Album
- `albumTrips` 只展示在 archive DB 中有对应 `trip_id` 的 trip
- 无 album 时展示空状态提示

### "Manage Photos" 按钮自动化
- **已链接**: 直接跳转 `/archive`；若无 album 则先静默同步再跳转
- **未链接**: 弹出文件夹选择器 → 创建/加载 `trip_database.json` → 同步所有 trip 数据 → 跳转
- 新增 `initAndSync()` 函数，一键完成初始化+同步

### Album 导航改用 tripId
- AlbumCard 点击导航从 `?tripTitle=` 改为 `?tripId=`
- Archive App 优先用 `tripId` 匹配，fallback 保留 `tripTitle` 兼容

---

## 3. 右键菜单修复 (ContextMenu)

### 子菜单滚轮修复
- `useContextMenu` 的 `scroll` 事件监听（capture 阶段）会误关闭菜单
- 修复：检查 `e.target` 是否在 `[data-context-menu]` 内，内部滚动不触发关闭
- 子菜单滚动容器添加 `onWheel={stopPropagation}` + `overscrollBehavior: contain`

### Event 选中状态修复
- `handleItemContextMenu` 用 `item.id` 生成 key，但 event 的 id 字段是 `event_id`
- 修复：根据 `item.type` 取正确的 id 字段（`trip_id` / `event_id`）

---

## 模块文件清单

| 模块 | 文件 |
|------|------|
| **Sync Hook** | `src/hooks/useArchiveSync.js` |
| **Album Grid** | `src/components/dashboard/TripAlbumGrid.jsx` |
| **Archive App** | `src/archive/App.jsx` |
| **Context Menu** | `src/archive/components/ContextMenu.jsx` |
| **Context Hook** | `src/archive/hooks/useContextMenu.js` |
