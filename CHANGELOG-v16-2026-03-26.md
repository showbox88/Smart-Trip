# 地址卡 3D 翻转 · 私密文档中心 — 2026-03-26

> **scope**: StopCard 3D 翻转交互优化、Private Note、Photos 附件功能、i18n 补全  
> **files changed**: `StopCard.jsx`, `DaySection.jsx`, `ItineraryView.jsx`, `itinerary.css`, `en.json`, `zh.json`

---

## 1. 地址卡 3D 翻转背面重设计

### 2×2 功能入口网格
- 将翻转背面从占位样式升级为 4 个功能入口（2×2 网格）：
  - **Photos** — 拍照或从相册选择图片
  - **Private Note** — 私密文字备注
  - **Web Link** — 确认/预订链接
  - **QR Code** — 二维码凭证（入口预留）
- 移除了之前的"QR Code / PDF / More Photos"临时占位内容。

### 对齐修复
- 将 `margin-left` 从 CSS 移到行内样式，确保地址卡与 NoteCard、ListCard 在时间轴上垂直对齐。
- 清理 `itinerary.css` 中冗余的 `margin-left` 定义及空 CSS 规则集。

---

## 2. Private Note — 私密备注底部面板

### 功能
- 点击背面 **Private Note** 图标，从屏幕底部弹出 Sheet（仅手机端）。
- 支持独立 **标题** 字段（简短描述，如"酒店确认号"）。
- 支持长文 **内容** 字段（确认码、密码、提醒等敏感信息）。
- 保存时通过 `onUpdateStop` 将 `privateNoteTitle` 和 `privateNote` 存入 stop 数据。
- 标题副标题显示当前地点名，明确所属关系（`📍 {stop.location}`）。
- 点击背景且有未保存改动时，弹出确认提示防止误操作。

### 动画
- 背景：`fadeIn 0.2s`。
- 面板：`slideUp 0.28s cubic-bezier(0.32, 0.72, 0, 1)`（iOS 系原生风格）。

---

## 3. Photos — 照片附件底部面板

### 功能
- 点击背面 **Photos** 图标打开相册面板。
- **拍照按钮**：调用手机后置摄像头（`capture="environment"`）。
- **从相册选择**：支持多选（`multiple`）。
- **云端存储路径**：`{userId}/{tripId}/{stopId}/attachments/{timestamp_random.ext}`（Supabase `trip-media` bucket）。
- 上传成功后即时同步至 stop 数据（`stop.attachments[]` 数组）。
- 支持 **3×3 缩略图预览网格**，点击任意图片可全屏查看。
- 支持单张 **删除**：同步从 Supabase Storage 删除文件，并更新 stop 数据。
- 上传中显示旋转 `sync` 动画图标。

---

## 4. 数据层与 Props 链路

- 在 `StopCard` props 解构中新增 `onUpdateStop`（修复了 Save 无反应的 bug）。
- `ItineraryView` → `DaySection` → `StopCard` 完整打通 `onUpdateStop={updateStop}` 链路。
- `stop.attachments`：`[{ url, path, name, createdAt }]` 结构，存储在行程 JSON 数据中。
- `stop.privateNoteTitle` / `stop.privateNote`：与地点 ID 绑定的私密字段。

---

## 5. i18n 国际化补全（中英文）

新增词条：

| key | 中文 | English |
|-----|------|---------|
| `itinerary.back_photos` | 截图或拍照 | Photos |
| `itinerary.back_private_note` | 私密备注 | Private Note |
| `itinerary.back_web_link` | 确认链接 | Web Link |
| `itinerary.back_qr_code` | 二维码凭证 | QR Code |
| `itinerary.swipe_back` | 右滑返回 | Swipe back |
| `itinerary.additional_docs` | 私密信息与文档 | Documents & Info |
| `itinerary.take_photo` | 拍照 | Take Photo |
| `itinerary.choose_photo` | 从相册选择 | Choose from Library |
| `itinerary.no_attachments` | 还没有附件... | No attachments yet. |
| `itinerary.private_note_title_label` | 标题 | Title |
| `itinerary.private_note_content_label` | 内容 | Content |
| `common.uploading` | 上传中... | Uploading... |
| `common.upload_failed` | 上传失败，请重试 | Upload failed, please try again |
| `common.discard_changes` | 放弃未保存的内容？ | Discard unsaved changes? |

---

## 模块文件清单

| 模块 | 文件 |
|------|------|
| **地址卡组件** | `src/components/itinerary/StopCard.jsx` |
| **天组件** | `src/components/itinerary/DaySection.jsx` |
| **行程视图** | `src/components/itinerary/ItineraryView.jsx` |
| **行程样式** | `src/styles/itinerary.css` |
| **中文词库** | `src/i18n/zh.json` |
| **英文词库** | `src/i18n/en.json` |
