# Smart Trip 使用手册 (User Guide)

欢迎使用 Smart Trip！本文档将引导您从零开始，快速掌握这款智能旅行规划工具的操作流程。

---

## 1. 启程：登录与创建行程
### **步骤示例：创建您的第一个日本之旅**
1.  **登录**：在首页中间的输入框输入您的名字（如 "Aiden"），按下 **Enter** 键或点击“开始规划”。
2.  **管理后台**：进入看板页面，点击右上角的 **"+ 新建行程"** 按钮。
3.  **填写信息**：
    *   在弹出的窗口中输入行程名称：“东京&京都漫步”。
    *   在日期选择器中选择起始与结束日期（例如 4月1日 - 4月7日）。
    *   点击 **"创建并打开"**。

### **步骤示例：筛选与切换视图**
1.  **状态过滤**：在仪表盘顶部，点击“全部行程”、“进行中”、“计划中”或“已完成”，列表将瞬间过滤目标。
2.  **切换视图**：点击右侧布局图标，可在网格 (Grid) 与 列表 (List) 模式间切换。列表模式现已支持完整的菜单操作。

---

## 2. 编排：智能管理与侧边栏交互
### **步骤示例：灵活使用侧边栏**
1.  **折叠侧边栏**：点击侧边栏顶部的左箭头 **"<"**，导航栏将收缩为极简模式，仅显示天数图标（如 D1, D2）。
2.  **天数跳转**：点击天数图标或标题，右侧行程单将即时滚动到对应日期。

### **步骤示例：为“第一天”添加景点**
1.  **搜索地点**：在“第一天”区块底部的搜框输入 “东京塔”。
2.  **回车添加**：在下拉建议列表中选中目标并按 **Enter**。
3.  **结果**：卡片会自动出现，显示“X 站行程”及详细地点信息。

### **步骤示例：插入一段笔记或购物清单**
1.  **悬停插入**：将鼠标移动到两个地点卡片中间的缝隙处，会出现一个蓝色的 **"+"** 按钮。
2.  **选择类型**：点击 "+"，在弹出的菜单中选择 **"📄 笔记"** 或 **"≡ 清单"**。
3.  **编辑内容**：直接在生成的笔记框内输入“记得带充电宝”，或在清单中添加“药妆店必买”。

---

## 3. 视觉：封面美化与智能换图
### **步骤示例：更换一张你喜欢的地标美图**
1.  **触发换图**：点击任意地点卡片右侧的小缩略图。
2.  **查看实拍**：窗口会自动从 Google Maps 抓取关于该地点的真实照片，平铺在网格中。
3.  **上传本地图**：如果您想使用自己的照片，在弹窗顶部点击“上传本地图片”，支持 PNG/JPG 格式，封面会自动适配模糊底衬以保证完整显示。
4.  **高效选择**：
    *   **单击**：选中某张图，观察其橙色边框预览。
    *   **双击**：如果您一眼看中某张图，直接**快速双击**，它将自动保存并关闭窗口。

---

## 4. 细节：财务与时间调整
### **步骤示例：追踪一次美食消费**
1.  **打开费用**：点击卡片底部的 **"$ 添加费用"** 蓝色标签。
2.  **设置图标**：在弹出的图标网格中点击 **"🍴"**。
3.  **输入金额**：点击输入框，系统会自动全选现有数字，您可以直接输入 "150"。
4.  **保存**：点击 **"确认"**。您可以注意到，卡片上的费用标签现在变成了绿色显示 "$150.00"。

---

## 5. 住宿：酒店管理与视觉协同
### **步骤示例：管理您的酒店住宿**
1.  **添加酒店**：在地点详情面板中，如果地点属于“酒店”类型，点击 **"+ 添加到行程"** 后的卡片将具有入住信息按钮。
2.  **设置入住/退房**：
    *   点击卡片上的 **"入住信息"** 按钮。
    *   在弹出的日历中选择 **入住日期与时间** 和 **退房日期与时间**，然后保存。
3.  **结果**：
    *   **语义化标签**：入住 (Check-in) 卡片顶部会显示**暖黄色**标签，退房 (Check-out) 卡片显示**警示红色**标签。
    *   **协同高亮**：鼠标悬停在酒店卡片、边框、插座或任意一段订阅线上，整段行程（跨越全天）的所有元素会同步进入“亮橙色”点亮状态。
    *   **跨天穿透连线**：系统会自动生成贯穿多日的订阅线，且线条会穿透日期标题，保持视觉流的完整。

---

## 6. 出行：路程统计与回程 Switch 开关
### **步骤示例：查看每日汇总**
1.  **真实统计**：在每天日程的最下方，系统会自动汇总当天的 **"总用时"** 和 **"总里程"**。
2.  **模式切换**：点击卡片间的 **🚗** 或 **🚶** 图标，统计数据会实时重算。

### **步骤示例：控制地图上的回程路线**
1.  **找到开关**：在汇总数据后方，您会看到一个现代化的 **Switch 开关**。
2.  **视觉控制**：
    *   **开启 Switch**：地图会补全从最后一站回酒店的连线。
    *   **关闭 Switch**：地图回程连线消失，保持路线简洁。

---

## 7. 地图：联动与探索
### **步骤示例：通过地图发现周边的咖啡馆**
1.  **切换视角**：点击页面顶部的“灵感”或直接操作右侧地图。
2.  **分类搜索**：点击地图顶部的 **"☕ Coffee"** 按钮，地图会立即显示当前区域内的咖啡馆。
3.  **地标交互**：点击地图上已经生成的数字图标（如 "Pin 1"），点击圆球、数字或外框任何位置均可立即弹出详情。
4.  **历史数据兼容**：即便您的旧行程站点没有预存 ID，系统也会在地标点击时自动通过名称检索并打开面板。
5.  **跨端添加**：在详情面板底部的下拉菜单中选择 **"添加到 第一天"**。该地点会立即同步进您的行程单。

---

## 8. 进阶：重排与个性化
### **步骤示例：调整游览顺序**
1.  **拖拽排序**：鼠标按住卡片左侧的图标或卡片空白处，将其上下拖动。
2.  **同步更新**：地图路线、卡片序号以及中间的交通时间都会在瞬间自动重算并更新。

---

## 9. 退出
*   点击页面顶部的您的用户名，即可登出并返回登录界面。

---
*Generated by Antigravity AI Assistant | 2026-03-11*
