# Changelog v20 — 2026-03-27

## Day-Centric Architecture Refactor (Phase 1)

### 架构重构
- **新数据模型**：以"天"为独立原子单元，每位用户每天全局唯一一条 `days_v2` 记录
- **Trip 降级为过滤器**：Trip 不再存储行程内容，仅作为标签聚合多个 Day（多对多关系）
- **三张新表**：`days_v2`（天的原子数据）、`trip_days`（多对多关联）、旧 `trips` 表扩展新字段（`start_date`、`end_date`、`settings`）
- **影子开发**：所有重构代码在 `feature/day-centric-refactor` 分支运行，不影响线上

### 新 Hooks
- `useDays`：`days_v2` 表的完整 CRUD，含懒创建（用户首次写入某天才建记录）
- `useTripsV2`：Trip 元数据 + `trip_days` 关联的增删查，支持多对多绑定

### 新功能：今日打卡
- Dashboard 新增"今日打卡"按钮，直接进入当天行程页面（`/day/YYYY-MM-DD`）
- 进入后地图自动请求 GPS 定位，中心移至当前位置
- 底部弹出"附近打卡"面板，列出 500m 内推荐地点
- 分类筛选：全部 / 餐饮 / 咖啡 / 景点 / 购物 / 酒店
- 点击"打卡"直接添加地址卡，时间记录为实际打卡时刻（非计划时间）
- 已打卡地点显示"已打卡"标记，防止重复添加
- 面板保持开启，可连续打卡多个地点

### 打卡模式 UI 适配
- `TripHeader`：打卡模式下隐藏"编辑行程 / 分享 / 删除"菜单
- `TripSidebar`：打卡模式下隐藏"添加天 / 删除天"按钮
- `ItineraryView`：接受 `isDayMode` prop，向下传递给 MapPanel、TripHeader、TripSidebar

### 存储层拦截
- `useTrips.saveTrip`：检测到虚拟 day trip（`_isVirtualDay` 标记）时，自动改写为 `saveDayToDB`，写入 `days_v2` 而非 `trips` 表
- `useTripEditor.addStopFromPlace`：新增 `useNow` 参数，打卡时使用实时时间

### 新组件 / 页面
- `DayPage`：单天行程入口，将 day 对象包装为虚拟 trip 注入 AppContext，复用全部现有 UI
- `NearbyCheckinPanel`：附近打卡底部面板组件

### 数据库
- `supabase/migration_v2.sql`：三张新表的完整 DDL + RLS 策略

### i18n
- 新增中英文翻译 key：`today_checkin`、`nearby_checkin`、`checkin_add`、`checkin_added`、`checkin_cat_*`、`no_nearby`
