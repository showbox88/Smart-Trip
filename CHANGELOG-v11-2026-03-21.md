# 侧边栏天数拖拽排序 — 2026-03-21

> **scope**: 侧边栏导航天数拖拽重排序功能
> **files changed**: `TripSidebar.jsx`, `ItineraryView.jsx`, `useTripEditor.js`, `useSidebarDrag.js`（新增）

---

## 1. 侧边栏天数拖拽排序 (Sidebar Day Drag-to-Reorder)

左侧导航栏的每一个 Day 条目现在支持拖拽排序，交互效果与时间线上的景点卡片完全一致：

- **拖起**：被拖动的 Day 行放大至 1.03 倍 + 深色投影浮起，光标变为抓取状
- **位移动画**：其余 Day 行在拖动过程中实时平滑让位（`cubic-bezier(0.25, 0.1, 0.25, 1)` 动画）
- **30% 重叠阈值**：与时间线保持一致，拖动超过相邻行高度 30% 即触发换位
- **松手提交**：释放鼠标后立即将新顺序写入行程数据并自动保存

---

## 2. 技术实现

### 新 Hook：`useSidebarDrag.js`
完整复刻 `useTimelineDrag.js` 的指针事件拖拽体系，针对侧边栏 `<li>` 元素适配：
- `onPointerDown` / `onPointerMove` / `onPointerUp` 三件套挂载在每个 Day 行上
- `setPointerCapture` 确保鼠标移出侧边栏范围后事件依然持续响应
- 触控端（iOS/Android）复用 `document` 级 `touchmove` 拦截，防止页面滚动干扰

### `moveDay`（`useTripEditor.js`）
新增天级别的移动函数：
- 将指定 Day 的所有行程内容移动到目标位置
- 日历日期与槽位绑定（stops 移动，日期不随之偏移），保持行程逻辑一致

### 防误触
- 拖动结束后 `onClick` 被阻断，不会误触发"跳转到当天"
- 拖动中 hover 高亮效果暂停，避免视觉抖动
