# Storage 清理修复 & 步行路线地图 — 2026-03-26

> **scope**: Stop 删除级联清理、Storage Optimizer 竞态修复、地图步行路线虚线渲染
> **files changed**: `useTripEditor.js`, `AdminPage.jsx`, `MapPanel.jsx`, `formatters.js`

---

## 1. 删除地点时自动清理 Storage 图片

- **根本修复**: `deleteStop` 现在在移除 stop 前先收集其 `stop.photo` 的 Storage 路径，删除后 fire-and-forget 调用 Supabase Storage `remove()`，防止孤立图片积累。
- **酒店 stay 联动**: 删除含 `stayId` 的 stop（checkin/checkout 成对）时，同样清理所有关联 stop 的图片。

## 2. Storage Optimizer 竞态修复

- **问题**: 添加地点后立刻扫描，图片已上传到 Storage 但数据库尚未写入（1.5 秒防抖延迟），导致新图片被误判为孤立文件。
- **修复**: 扫描时自动跳过 10 分钟内上传的文件，状态栏会提示跳过数量，彻底消除误删风险。

## 3. 地图步行路线虚线渲染

- **功能**: 点击地点卡之间的步行图标后，地图路线更新为步行模式（Google Routes API `WALKING`），并以虚线样式展示。
- **分段渲染**: 将 routePath 按 `transitMode` 分段，每段独立调用路线 API 并用对应样式绘制（驾车=实线，步行=虚线）。
- **缓存隔离**: `transitHelpers` 缓存键包含 mode（`DRIVE`/`WALK`），切换模式不会读到旧缓存。

## 4. 清理 formatters.js 死代码

- 移除文件末尾的 `getTripStays` 冗余包装函数及其对应的 bottom-of-file `import`（反模式），确认无外部调用者。

---

## 模块文件清单

| 模块 | 文件 |
|------|------|
| **Stop 删除** | `src/hooks/useTripEditor.js` |
| **Storage 扫描** | `src/pages/AdminPage.jsx` |
| **地图路线** | `src/components/itinerary/MapPanel.jsx` |
| **工具函数** | `src/utils/formatters.js` |
