# Admin 控制台清理工具升级 — 2026-03-26

> **scope**: AdminPage 无用附件扫描、清理安全防护与缩略图交互  
> **files changed**: `AdminPage.jsx`

---

## 1. 递归扫描与深度防护

- **递归子目录扫描**: 重写了 Storage 扫描逻辑（新增 `listAllFiles`），现在能穿透根目录，递归扫描所有深层子目录文件（如 `userId/tripId/stopId/attachments/`）。
- **附件类型识别**: 查询数据库时，将 `stop.attachments[]` 中的图片路径纳入“已使用(used)”名单，彻底消除误删行程附件文件的风险。
- **全路径精准对比**: 将比对逻辑从容易重名的“仅比较文件名”升级为比对“文件全路径”，增强准确性。

## 2. 交互与可视化升级（缩略图版回收站）

- **可视化网格布局**: 将原本平淡的文本列表升级为高颜值的多列缩略图网格。
- **看图确认防误删**: 支持点击放大镜图标在独立 Lightbox 浮层中放大预览图片。
- **自主勾选机制**: 将“一键全部删除”改为“复选框自主勾选”，提供「全选 / 取消全选」便捷操作。
- **非图片文件兜底**: 其他类型附件提供通用图标（File Icon）展示方案，并能正确截断过长文件名。

## 3. 行程删除级联清理 (Cascade Delete)

- **完善清理逻辑**: 当管理员在 Admin 后台手动点击“Delete”删除某个行程时，新增级联清理动作：
  1. 先检索该行程所有天、所有地点下的 `attachments` 路径。
  2. 批量从 Supabase Storage 中清理这些文件。
  3. 最后删除数据库主记录。
- 新增弹窗动态提示：“行程已删除，同时清理了 N 个附件文件”。

---

## 模块文件清单

| 模块 | 文件 |
|------|------|
| **控制台大盘** | `src/pages/AdminPage.jsx` |
