# 酒店线渲染修复与稳定性增强 — 2026-03-22

> **scope**: 酒店住宿线（amber hotel line）多酒店场景渲染修复、弧线断点消除、hotelContext 缓存优化
> **files changed**: `DaySection.jsx`, `StopCard.jsx`

---

## 1. 多酒店场景酒店线不刷新修复 (Multi-Hotel Re-render Fix)

**问题**：添加第一个酒店后，入住/退房之间的天数酒店线显示正常；但在行程后段再添加第二个酒店时，新酒店相关天数的工具栏（Auto Fill / Optimize Route）和底部 AddStopRow 不显示酒店线。

**根因**：`DaySection` 内部 `hotelContext` 使用 `useMemo(() => getHotelContext(day, trip), [day, trip])` 缓存计算结果。虽然 React memo 因 inline arrow function（`onToggleCollapse`、`onSortByTime`）每次都会 bypass，但 `useMemo` 依赖判断在特定场景下可能持有旧的 `hotelContext` 快照，导致新加酒店的 isBetween 天无法反映最新的住宿范围。

**修复**：将 `hotelContext` 的计算由 `useMemo` 改为每次渲染直接调用（无缓存）：

```diff
- const hotelContext = useMemo(() => getHotelContext(day, trip), [day, trip]);
+ const hotelContext = getHotelContext(day, trip);
```

由于 `DaySection` 已通过父组件内联 prop 保证每次都重新渲染，`getHotelContext` 的轻量计算（纯数组遍历，无副作用）完全可以在每次渲染时重新执行，不存在性能问题。

---

## 2. 弧线端点断点修复 (Arc-to-HotelLine Gap Fix)

**问题**：入住/退房卡片的弧线（arc connector）与竖向酒店线之间存在视觉断点（`断点`）。

**根因**：弧线 div 使用 `borderTopLeftRadius: 8px`，高度仅 8px，左边框因 CSS 圆角曲线渲染，在亚像素级别与 HotelLine div 的起始位置（`calc(50% + 8px)`）之间存在微小缝隙。

**修复**：将 HotelLine 的起点/终点调整为与弧线起点相同，使两者重叠 8px，彻底消除任何亚像素断点：

```diff
  // hotel_checkin 卡片
- top={stop.type === 'hotel_checkin' ? 'calc(50% + 8px)' : '-1rem'}
+ top={stop.type === 'hotel_checkin' ? '50%' : '-1rem'}

  // hotel_checkout 卡片
- bottom={stop.type === 'hotel_checkout' ? 'calc(50% + 8px)' : '-1rem'}
+ bottom={stop.type === 'hotel_checkout' ? '50%' : '-1rem'}
```

弧线左边框与 HotelLine 重叠区域颜色/宽度完全一致（均为 `var(--hotel-line-color)` / `var(--hotel-line-width)`），视觉上天衣无缝。

---

## 技术细节

| 变更点 | 文件 | 说明 |
|---|---|---|
| `hotelContext` 去缓存化 | `DaySection.jsx` | 改为每次渲染直接调用 `getHotelContext` |
| 弧线起点对齐 | `StopCard.jsx` | checkin top / checkout bottom 改为 `'50%'` |
