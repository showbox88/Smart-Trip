# 相册国际化支持 & 行程编辑体验优化 — 2026-03-20

> **scope**: 相册分类翻译与图标同步、行程单按钮样式优化、地图自动中心偏移、清单/笔记自动聚焦
> **branch**: `refactor/itinerary-v3-core`
> **files changed**: 11+ files

---

## 1. Photo Archive 深度国际化与视觉统一

### 改进点
- **分类多语言支持**：实现了相册分类（如美食、景点、街景等）的完整中英文翻译映射，支持跟随系统语言动态切换。
- **图标与颜色同步**：分类图标（Lucide 图标集）现在会自动继承在属性管理器中定义的分类颜色，保持了视觉语言的高度一致。
- **看板标签汉化**：修复了主界面“行程相册”区域及连通状态（已链接/未链接）的硬编码中文字符。

---

## 2. 地图 UI 与交互体验优化

### 改进点
- **气泡中心偏移**：解决了地图下方弹出详情页时遮挡标记的问题。现在标记会自动移动到上方可视区域的中心。
- **地址卡片（StopCard）精简**：
  - 缩小了“预约时间”和“添加消费”按钮的尺寸。
  - 统一颜色规范：预约时间采用 **橙色**，添加消费采用 **绿色**。
- **悬浮缩放修复**：修复了在地图标记上 Hover 导致地图异常缩放或位移的冲突 Bug。

---

## 3. 行程单编辑体验 (UX) 提升

### 改进点
- **快速创建流程**：在清单（Checklist）标题输入框按 **回车 (Enter)**，会自动跳转到首个任务输入框。如果清单为空，则自动新建任务并聚焦。
- **自动进入编辑状态**：点击“添加笔记”或“添加清单”后，页面会自动滚动到新项并**直接进入输入状态**，无需二次点击。
- **实时保存修复**：解决了清单标题在修改过程中无法实时同步到数据库的问题。

---

## 4. 关键 Bug 修复

- **Vite 500 错误**：修复了 `zh.json` 中的一个 JSON 语法错误（多余的逗号），该错误曾导致开发服务器频繁崩溃。
- **引用错误**：修复了 `NoteCard.jsx` 中缺失 `useEffect` 导入导致的运行期崩溃。

---

## 5. 文件变动统计

| 模块 | 主要文件 |
|------|---------|
| **i18n** | `zh.json`, `en.json` |
| **Photo Archive** | `PhotoCard.jsx`, `ContextMenu.jsx`, `TripAlbumGrid.jsx` |
| **Itinerary** | `ItineraryView.jsx`, `ListCard.jsx`, `NoteCard.jsx`, `DaySection.jsx` |
| **Styling** | `layout.css`, `Timeline.css` |

---
*Generated 2026-03-20 on branch `refactor/itinerary-v3-core`*
