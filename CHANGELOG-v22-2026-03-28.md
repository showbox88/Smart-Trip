# Changelog v22 — 2026-03-28

## 新功能：日历视图（Calendar View）

### 新页面 / 新组件
- **`CalendarPage.jsx`**：日历主页面，内嵌于 Dashboard trip-grid 区域，无需跳转页面
- **月视图（Month View）**：7 列日历网格，6 周行
  - 每天格子可点击 → 跳转 `/day/YYYY-MM-DD`
  - Trip 显示为跨日期彩色条带（贪心算法多行堆叠，防止重叠）
  - 点击 Trip 条带 → 跳转对应行程（V1: `/trip/:id` / V2: `/trip-v2/:id`）
  - 每天显示：`● 📍 N stops`（颜色点 + 地标图标 + stop 数量）
  - 今天日期蓝色圆形高亮
- **年视图（Year View）**：4×3 缩略月格，全年活动密度一览
  - 橙色格：该天在 Trip 日期范围内
  - 淡蓝格：该天有打卡记录（days_v2 有 stops）
  - 亮蓝格（发光）：trip 范围 + 有打卡记录
  - 正方形格子，宽度 = (月框内容宽 - 16px) ÷ 7，`aspect-ratio: 1`
  - 月份框 `aspect-ratio: 1` 保持正方形
  - 点击月份缩略图 → 切换至对应月的月视图
  - 底部 Trip 图例，可点击跳转行程

### Dashboard 集成
- **`DashboardFilters.jsx`**：视图切换栏新增日历图标（第四个）
  - 点击 → `dashboardView = 'calendar'`，日历在 trip-grid 区内嵌渲染
  - 激活时图标高亮，与网格/列表/相册图标一致
- **`App.jsx`**：新增 `/calendar` 独立路由（备用直接访问）
- **`index.css`**：引入 `calendar.css`
- **i18n**：新增 `calendar.*` 中英文翻译键（title / month_view / year_view / today / no_activities）

## Bug 修复

### 修复编辑 V2 Trip 名称后列表出现重复卡片
- **根本原因**：`handleSaveEditedTrip` 对 V2 trip 调用 `saveTrip`，导致虚拟 trip 被加入 `state.trips`，与 `state.tripsV2` 中同一条目形成重复
- **修复**：V2 trip 编辑时改为直接调用 `useTripsV2.updateTrip`，绕过 `saveTrip`，不污染 `state.trips`
- **影响文件**：`DashboardPage.jsx`
