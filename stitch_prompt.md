# 🚀 Smart Trip | Stitch UI 设计提示词 (Full Detail Prompt)

**角色：** 资深 UI/UX 专家，擅长现代、沉浸式、信息量大且极简的界面设计。

**任务：** 为我的旅行规划应用 "Smart Trip" 设计（或重构）一套高标准的 UI 界面。你需要基于现有逻辑生成具有“Premium”质感的界面，注重玻璃拟态（Glassmorphism）、动态主题和直观的交互路径。

---

## 1. 产品背景与设计调性 (Core Aesthetics)
- **产品定位：** 针对旅行爱好者、高频出行者的沉浸式行程管理助手。
- **视觉风格：** Dark Theme (深色主题), 高级感, 极致轻盈, 极客范儿。
- **配色规范 (Hex)：**
    - 背景：`#0d111b` (大背景) / `#1a2333` (行程编辑区深蓝色背景)
    - 强调色：`#f97316` (活力橙, 用于地点/图标) / `#3b82f6` (科技蓝, 用于交通/按钮)
    - 交互色：玻璃拟态 `rgba(30, 41, 59, 0.7)`，边框 `rgba(255, 255, 255, 0.1)` 带有轻微模糊。
- **字体推荐：** `Outfit` (标题) & `Noto Sans SC` (正文)，呈现现代简洁感。

---

## 2. 核心界面布局 (UI Layout)
### A. 全局概览看板 (Dashboard)
- **行程卡片：** 采用带间隙的网格布局。每张行程卡片需展示：目的地高清缩略图、日期范围（`startDate` 至 `endDate`）、状态标签（进行中、已计划）、预算概览、地点总数统计。
- **操作：** 显著的 “+ 新建行程” 按钮，支持快速录入。

### B. 沉浸式行程编辑器 (Itinerary Editor - 核心)
- **左侧导航：** 极简的侧边栏，显示每日日期（如 D1, D2），带主题色圆点。点击可平滑滚动至该天。
- **中间：垂直虚线时间轴 (Timeline)：**
    - **逻辑：** 每日行程通过连续的虚线串联。虚线颜色根据该天用户自定义的颜色平滑过度。
    - **时间线 Pin 点：** 位置卡片左侧带有数字序号的图钉（Pin）。图钉需具有发光特效（Neon glow），颜色与当日主题色同步。
- **行程卡片 (Stop Cards)：**
    - **玻璃拟态美学：** 带有背景模糊（Backdrop Filter）和 1px 半透明描边。
    - **富媒体展示：** 右侧带有圆角地点实拍图；左侧为地点名称、Google 评分、分类图标。
    - **元数据微章：** 时间、费用、备注采用微缩 Chip 设计，色彩柔和但不显杂乱。
- **酒店逻辑可视化：**
    - **Check-in/out：** 具有特定颜色边框（入住绿/退房红）。
    - **酒店区间：** 跨天住宿时，在卡片边缘显示细长的垂直指示条（Indicator Bar），暗示“正在入住中”。
- **交通分段：** 两站之间显示细小的交通图标（🚗/🚶）、距离和时长，鼠标悬停可触发“+”快捷插入按钮。

### C. 增强型地图视图 (Map Integration)
- **双模皮肤：** 默认暗黑模式（Night Mode），可一键切换至明亮模式。
- **智能路线：** 地图路线颜色必须与行程单中当天的“主题色”像素级匹配。
- **非模态详情面板：** 点击地图 POI 弹出不干扰整体视角的侧滑或悬浮窗口，可一键“+ 添加到当前天”。

---

## 3. 核心交互逻辑 (UX Details)
- **拖拽排序 (DnD)：** 卡片在拖拽时应有阴影加深效果，背景虚化程度变化。
- **自适应输入：** 所有笔记和列表卡片采用无边框设计，随着输入字数自动撑开高度（Auto-expanding）。
- **非模态确认：** 所有的删除、确认操作不应导致全屏变黑，而是在点击位置弹出轻量级的“气泡式确认框”。
- **状态感知：** 页面左下角需有地图引擎、数据同步的实时状态指示灯（脉冲动效）。

---

## 4. 关键输出要求 (Execution Output)
1. **配色细节：** 请在 CSS 变量层面提供完整的调色盘。
2. **渐变效果：** 为每日行程提供一套和谐的渐变色板，用于路线渲染。
3. **响应式设计：** 适配桌面端宽屏（地图/行程左右分栏）且兼容移动端卡片流。
4. **图标系统：** 使用高度一致的填充式或线性图标集（如 Lucide 或 FontAwesome）。

**Stitch，请基于以上细节，为 Smart Trip 呈现一个能够让用户“一眼惊艳”的现代旅行助手界面！**
